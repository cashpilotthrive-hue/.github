# Deployment Guide
Use managed PostgreSQL, managed Redis, containerized FastAPI, static admin hosting, and EAS Build for mobile. Rotate JWT and payment provider secrets through a cloud secrets manager. Enable HTTPS, WAF/rate limiting, database backups, observability, and audit retention before production.
